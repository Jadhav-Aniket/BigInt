// generate_input.h
#ifndef GENERATE_INPUT_H
#define GENERATE_INPUT_H

#include <vector>
#include <string>

void generate2DVector(size_t len, size_t size, std::vector<std::vector<std::string>> &matrix);

#endif // GENERATE_INPUT_H